#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry level application
 * Return: Always (0) (Sucess)
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
		putchar (n);
	putchar ('\n');
		return (0);
}
